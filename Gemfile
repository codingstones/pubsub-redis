source 'https://rubygems.org'

git_source(:github) { |repo_name| "https://github.com/#{repo_name}" }

gem 'pubsub', git: 'https://github.com/codingstones/pubsub'

# Specify your gem's dependencies in pubsub-redis.gemspec
gemspec
