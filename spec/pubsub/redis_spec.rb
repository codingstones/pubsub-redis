require 'spec_helper'

RSpec.describe Pubsub::Redis do
  it 'has a version number' do
    expect(Pubsub::Redis::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
